[workspace]
members = ["core", "bindings"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/aryanjp1/tracelift"

[profile.release]
lto = "thin"
codegen-units = 1
